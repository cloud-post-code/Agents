[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["cd backend && pip install -e '.[test]'"]

[phases.build]
cmds = ["cd backend && alembic upgrade head"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
