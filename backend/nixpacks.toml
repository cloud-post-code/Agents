[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = ["pip install -e '.[test]'"]

[phases.build]
cmds = ["alembic upgrade head"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
